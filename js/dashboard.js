setTimeout(function () {
    const soldItemsCount = 100;
	const totalUsersCount = 500;

    document.getElementById("sold-items-count").textContent = soldItemsCount;
    document.getElementById("total-users-count").textContent = totalUsersCount;
}, 2000);
